<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.univates.meuapp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">
        <activity
            android:name=".foradeuso.CalculadoraActivity"
            android:exported="false" />
        <activity
            android:name=".CadastroLinguagensActivity"
            android:exported="false" />
        <activity
            android:name=".ListaLinguagensActivity"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA4Hn9B5Mr2Lm2htLlL7NGdPBHgCdh6kDQ" />

        <activity
            android:name=".foradeuso.MapsActivity"
            android:exported="false"
            android:label="Mapas" />
        <activity
            android:name=".foradeuso.ListaActivity"
            android:exported="false" />
        <activity
            android:name=".foradeuso.TabuadaActivity"
            android:exported="false" />
        <activity
            android:name=".foradeuso.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".foradeuso.MainActivity"
            android:exported="true" />
    </application>

</manifest>